{
    "Logging": {
        "LogLevel": {
            "Default": "Information",
            "Microsoft": "${TEST}",
            "Microsoft.Hosting.Lifetime": "Information"
        }
    },
    "AllowedHosts": "*",
    "Settings": {
        "AllowedOrigins": "$ALLOWED_ORIGIN",
        "DpsGlobalDeviceEndpoint": "${ALLOWED_ORIGIN}",
        "DpsIdScope": "${TEST}",
        "EnrollmentGroupPrimaryKey": "",
        "EnrollmentGroupSecondaryKey": "",
        "IotHubConnectionString": "${TEST}"
    },
    "ConnectionStrings": {
        "Aem": "$AEM_CONNECTION_STRING",
        "EventHub": "${AEM_CONNECTION_STRING}"
    }
}
